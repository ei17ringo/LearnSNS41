ファイルを読み込んで実行できる関数

require
動作
エラーが起きた時に、処理を中断し、読み込み側のその後の処理も行わない

利用するとき
DBに接続など、エラーが起きたら致命的な処理の場合使用（その後の処理を行ってもあまり意味がない）

include
動作
エラーが起きた時に、処理を継続し、読み込み側のその後の処理も継続する

利用するとき
HTMLなど表示を行うときに使用（多少のエラーが有っても全体的に処理を行ったほうがユーザーに対してメリットが有る場合）